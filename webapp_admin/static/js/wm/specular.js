const REDUCED_MOTION=window.matchMedia("(prefers-reduced-motion: reduce)");export function attachSpecular(root=document){if(REDUCED_MOTION.matches)return;let pending=null;root.addEventListener("pointermove",(event)=>{const target=event.target.closest?.("[data-specular]");if(!target)return;if(pending)return;const{clientX,clientY}=event;pending=window.requestAnimationFrame(()=>{pending=null;const rect=target.getBoundingClientRect();if(!rect.width||!rect.height)return;target.style.setProperty("--spec-x",`${((clientX - rect.left) / rect.width) * 100}%`);target.style.setProperty("--spec-y",`${((clientY - rect.top) / rect.height) * 100}%`);});},{passive:true});}