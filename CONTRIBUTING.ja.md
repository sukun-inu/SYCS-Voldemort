# 開発の進め方

[← README.ja.md に戻る](README.ja.md)

English: [CONTRIBUTING.md](CONTRIBUTING.md)

このリポジトリで手を入れるときの約束ごとです。**どれも実際に事故った結果として
書かれています。** 理由まで書いてあるので、納得できないものがあれば理由の方を
疑ってください。

コードもコメントもコミットメッセージも日本語です。

---

## 1. 最初の1回だけ

```bash
pip install -r requirements.txt -r requirements-dev.txt

# 整形だけのコミットを git blame から外す
git config blame.ignoreRevsFile .git-blame-ignore-revs
```

2行目を忘れると、`git blame` の大半が「black を全ファイルへ適用した」コミットを
指してしまいます。このリポジトリでいちばん価値があるのは、原因まで書いてある
履歴なので、ここは必ず設定してください。

---

## 2. 変更の流れ

```bash
git checkout -b <fix|feat|refactor|test|ci|style|chore>/<内容>
# 直す
python -m unittest discover -s tests -t .
git commit                       # メッセージは 3. を参照
git checkout main
git merge --no-ff <ブランチ名>    # merge: で始まる1行
git push origin main
```

`--no-ff` を付けるのは、1つの作業が何コミットで構成されていたかを履歴に残す
ためです。

---

## 3. コミットメッセージ

**何が起きていたか → なぜそうなったか → どう直したか → どう検証したか**の順で
書きます。長さは気にしなくてよいので、後から読む人が原因を追えるようにして
ください。直近のコミットは平均40行を超えています。

とくに「なぜそうなったか」を省かないこと。同じ間違いを繰り返さないための情報は
そこにしかありません。実際、過去のコミットに書かれていた「Linux の本番だけで
書き出しが落ちていた」という一文が、後日まったく別の不具合を切り分ける手がかりに
なりました。

末尾に共同作業者の行を入れます。

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

---

## 4. コメントは「なぜ」を書く

コードを読めば分かることは書かないでください。書くのは、**読んでも分からない
こと**です。

```python
# 悪い例: 見れば分かる
# rowcount を int にして返す
return {"ok": True, "deleted": int(result.rowcount or 0)}

# 良い例: 外すと何が起きるかが分かる
# rowcount は返さないドライバがある。or 0 を外すと int(None) で
# TypeError になり、購読解除が 500 で落ちる。型検査を黙らせるために
# 外してよい保険ではない。
return {"ok": True, "deleted": int(result.rowcount or 0)}
```

「以前どう壊れていたか」は特に価値があります。将来それを「不要なコード」と
判断して消そうとする人を止められるのは、その一文だけです。

---

## 5. テスト

`pytest` は入っていません。**`unittest` を使ってください。**

```bash
python -m unittest discover -s tests -t .        # 全部
python -m unittest tests.test_services -v        # 1ファイル
```

### docstring に「なぜこのテストがあるか」を書く

```python
def test_tracks_are_padded_onto_one_timeline(self):
    """喋った時刻が違っても、全トラックが同じ長さ・同じ時間軸に揃うこと。

    受信できるのは発話中のパケットだけなので、素直に繋ぐと無音が詰まって
    トラック同士がずれる。ずれると重ねて編集できず、マルチトラックの意味が無い。
    """
```

何を検証しているかは読めば分かります。書くべきなのは、**それが壊れると何が
困るのか**です。

### 新しいテストは、壊して確かめる（変異テスト）

カバレッジの数字はテストの質を保証しません。全部呼んで何も検証しなければ、
99% でも出ます。

書いたテストが本当に何かを守っているかは、**守っているはずのものを実際に壊して
みる**以外に確かめる方法がありません。

```bash
# 1. 本番コードのコピーを取る（git checkout で戻すのは、他の変更まで消えるので危険）
# 2. 1箇所だけ壊す（条件を反転する、上限を1つずらす、ガードを消す）
# 3. 狙ったテストだけが落ちることを確認する
# 4. 必ず元に戻し、バイト単位で一致することを確認する
git status   # 本番コードが無変更であること
```

落ちなければ、そのテストは何も守っていません。書き直すか消してください。

過去の実例です。

| 壊した箇所 | 落ちたテスト |
|---|---|
| 管理者チェックを反転 | 13件（`*_refuses_non_admin` 系） |
| SQL コンソールの readonly を外す | 3件 |
| URL 上限を off-by-one に | 2件 |

### 長い関数を割る前に、不変条件テストを書く

100行超の関数を割るときは、**割る前に**「外から見た振る舞い」を固定するテストを
1本書き、**割る前のコードで通してください。**

順番がすべてです。割ったあとに書いたテストは、割った形に合わせたテストになるので、
**「割る前と同じか」については何も言えません。**

固定するものは、関数の性質で決まります。

| 性質 | 例 | 固定するもの |
|---|---|---|
| 組み立て | `create_app`、`register` 系 | 何がどの順で登録されたかの一覧。順序に意味があるなら順序ごと |
| 計算 | `forecast_for_metal` | 固定入力に対する出力（golden value） |
| 手続き | `_finalize`、`sync_guild_user_states` | 外へ出た作用の並び（どのファイルができたか、どのクエリが走ったか） |

**この不変条件テスト自体にも変異を当ててください。** 割る前のコードを1箇所壊して
落ちないなら、そのテストは不変条件を掴んでいません。掴んでいないテストを持って
割るのは、何も持たずに割るより危険です（守られているつもりになるため）。

なぜこの決まりがあるか。docstring を787本足したときは、AST から docstring を剥がして
突き合わせることで「それ以外を動かしていない」ことを毎コミット証明できました。
**関数を割るときには、それに相当する道具が無い。** だから、始める前に自分で用意します。

道具を持たずに割った結果も残っています。過去3回の分割では、割った先の `register()` が
116行・129行になり、**100行超の本数は 26 → 26 のまま1本も減りませんでした。** 長さが
移動しただけで、しかも「壊していない」ことを示すものが手元にありませんでした。

書いた不変条件テストは、割り終わったあとも残してください。次に同じ関数へ手を入れる
人の守りになります。

### モックが効いていることを確かめる

`patch()` したつもりで一切効いていない、という事故が実際にありました。

**FastAPI の `Depends()` はモジュール属性の `patch()` が効きません。**
`Depends(get_db_session)` は読み込み時に関数オブジェクトを束縛するため、後から
`webapp.app.get_db_session` を差し替えても、登録済みのルートは元の関数を呼び
続けます。`app.dependency_overrides` を使ってください。

一般に `mock.called` や `assert_called_with` で「実際に差し替わったか」を確認
してください。効いていないモックは、テストが本番の挙動を何も検証していない
ことを意味します。

### 出てはいけない先

実 DB とネットワークへは出ないこと。Postgres が無い環境でも全部通ります。
DB が要るものは、`services` 層を差し替えるか、同じ SQLAlchemy モデルを
インメモリ SQLite で動かしてください（`tests/test_user_state.py` が後者の例です）。

### `print()` を残さない

デバッグに使ったら消してください。CI のログが読めなくなります。

---

## 6. lint・整形・型検査

```bash
python -m ruff check .
python -m black .
python -m mypy -p webapp -p events -p services -p webapp_admin -p commands
```

設定は `pyproject.toml`。CI が3つとも回し、落ちたらジョブが落ちます。

**型検査を通すために、実行時の防御を外さないこと。** これが最も重大な禁止事項
です。実際に起きた事故:

```python
- return {"ok": True, "deleted": int(result.rowcount or 0)}
+ return {"ok": True, "deleted": int(result.rowcount)}  # type: ignore[attr-defined]
```

mypy が指摘したのは `.rowcount` の方なのに、外されたのは `or 0` でした。
`# type: ignore` は結局付いているので、**型検査は黙らせたまま実行時の保険だけ
失う**形になっています。

型を通したいときに選んでよいのは次の4つだけです。

1. 注釈を足す
2. `# type: ignore[エラーコード]` を付ける（**必ず1行の理由コメントを添える**。
   コード無しの裸の `# type: ignore` は禁止）
3. ローカルへ束ねて絞り込む（`guild = message.guild` など。判定は変えない）
4. `typing.cast(...)` を使う

条件式・文の順番・例外処理・既定値・シグネチャは変えないでください。**迷ったら
`# type: ignore` を選ぶこと。** 実行時に何も変わらない方が常に安全です。

### `# type: ignore` より、指摘されない書き方を先に探す

**エラーコードは環境で変わります。** 手元（Python 3.13）で通っても、CI（3.11）で
別のコードが出て落ちることがあります。実際に踏みました。

```python
# 手元では call-arg、CI では misc が出る。書き分けが要るうえ、
# warn_unused_ignores が有効なので余分なコードを足すのも危ない
return discord.Color.dark_gray()  # type: ignore[call-arg]

# 正規の綴りを呼べば、そもそも指摘されない（値は同じ）
return discord.Color.dark_grey()
```

`# type: ignore` を書く前に、**指摘されない等価な書き方が無いか**を一度探して
ください。無ければ ignore で構いませんが、その場合は CI の結果まで見届けること。
手元の mypy が Success でも、それは CI が通る証明にはなりません。

---

## 7. しきい値は「天井」であって目標ではない

| 場所 | 値 | 意味 |
|---|---|---|
| `.coveragerc` の `fail_under` | 69 | これを割ると CI が落ちる（実測 71.2%） |
| `pyproject.toml` の `max-complexity` | 23 | 現在の最大値に合わせた天井（`_build_search_queries` と `handle_security_for_message`） |
| CI の `mypy -p ...` | 本体5パッケージ全部 | 147ファイル |
| `tools/check_docstrings.py` の `FLOOR_PERCENT` | 100 | 本体の関数 1282 本すべてに docstring がある状態を保つ |

どれも「今より悪くしない」ための歯止めです。**テストを足したり関数を割ったり
したら、この数字も一緒に締めてください。** 緩めたままにすると、その分だけ静かに
劣化できる余地が残ります。

型検査の対象を広げるときは、そのパッケージの指摘を 0 にしてから `-p` を足して
ください。

---

## 8. CI が落とす8つの条件

`.github/workflows/tests.yml`。`main` への push と、すべての pull request で
走ります。

1. `python -m unittest discover -s tests -t .`
2. カバレッジが `fail_under` を下回らないこと
3. `ruff check .`
4. `black --check .`
5. `mypy -p webapp -p events -p services -p webapp_admin -p commands`
6. `tools/check_requirements.py`（依存の固定が Docker と同じ条件で解けるか）
7. `tools/check_admin_schema.py` と `tools/generate_admin_docs.py --check`
8. `tools/check_docstrings.py`（本体の関数に docstring があるか。下限 100）

8. は**数しか見ていません。** 空でない文字列が書いてあれば通るので、「読めば
分かることを書き写しただけの docstring」は素通りします。緑であることは、4. を
満たしている証拠にはなりません。

**「入れてはあるが、緑にするために素通りさせる」形にはしないでください。**
`tools/check_admin_ui.py` を CI に入れていないのは同じ理由です。playwright を
入れないまま置くと、import に失敗してスキップし、何も検証しないまま exit 0 に
なります。検証したふりだけが残る状態は、無いより悪いです。

---

## 9. AI エージェントに任せるとき

複数のエージェントに並行で作業させる場合の約束です。これも全部、実際に事故が
起きた結果です。

- **担当ファイルを重ならないように割り当てる。** 「このファイルだけ触ってよい」
  という形にする。
- **本番コードは変更させない。** バグを見つけても直さず報告させる。修正は、
  報告を読んでから別のコミットで行う。実際に「型検査を通すため」として防御
  コードが外された事例があります（6. 参照）。
- **変異テストを義務づけ、結果を報告させる。** これが「通るだけのテスト」への
  唯一の実効的な歯止めです。
- **変異テストは直列で行う。** 本番コードを一時的に書き換えるので、複数の
  エージェントが同時にやると衝突します。実際、セッション上限で1体が変異の
  途中で止まり、`services/djaudio_service.py` が壊れたまま残りました。
- **エージェントが途中で止まったら、まず `git status` を見る。** 本番コードが
  書き換わったまま残っていないかを最初に確認してください。
- **報告を鵜呑みにしない。** 数字は自分で測り直す。カバレッジ 99% の報告が
  「全部呼んで何も検証していない」ことは普通にあります。

### 1回で頼む単位と、頼み方

エージェントは**読んだぶんだけ手一杯になります。** 1,600行のファイルとそのテストで、
1回に読める量の大半が埋まります。調べさせてから考えさせるのではなく、狙いを決めて
から渡してください。

- **1回につき1つの塊。必ず `merge` まで終わらせる。** 途中で止まると、変異テストの
  ために書き換えた本番コードが残ります（実際に残りました。上の「まず `git status` を
  見る」はそのためにあります）。
- **読ませるファイルを閉じる。** 「100行超の31本を調べて」ではなく「`create_app` を
  割って」。調べること自体が目的なら、それだけを1回にする。
- **始める前に `git status` が clean であること。**

頼むときの型です。

```
<関数名> を割ってください。

先に不変条件テスト（<何を固定するか>）を書き、割る前のコードで通すこと。
そのテストに変異を1つ当てて、落ちることも確認すること。
そのあと割って、同じテストが通ったままであることを確認すること。
```

「割って」だけを渡すと、毎回この段取りから考え直すことになります。**段取りまで
指定したほうが、結果も速さも安定します。**

---

## 10. 迷ったとき

- 既定は安全側へ倒す。不都合は隠さない
- 分からない値は「不明」と書くのではなく、その行ごと出さない
- 黙って何もしない分岐を作らない。理由を1つに特定してログへ残す
- 同じ判定を2箇所に書かない。片方だけ直る取りこぼしの主因です
- 設定は、保存できるだけでなく読む側まで通す
- 外部データ源は使う前に利用規約を読む
