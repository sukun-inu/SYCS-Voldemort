# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# 仮想環境
.venv/
venv/
env/
.env

# 設定・シークレット
*.env
.env.*
!.env.example
data/
shared_secrets/

# IDE
.vscode/
.idea/
*.swp
*.swo

# ビルド
dist/
build/
*.egg-info/

# テスト
.pytest_cache/
.coverage
htmlcov/

# OS
.DS_Store
Thumbs.db

# 検証用の実音声（実際の会話。個人が識別されうるので絶対に公開しない）
sample-audio/

# 不具合報告用のスクリーンショット（管理画面の中身が写るので公開しない）
bug-image/

# Chromium（Playwright の検査）が吐くクラッシュ報告のログ
debug.log

# Claude のメモリは ~/.claude 配下に置く。リポジトリには入れない
# （過去に2度、ここへ書き込まれてコミットまでされた）
memory/

# 手元での確認に使う実録音。**絶対に追跡しないこと。**
# 実在の利用者の声そのもので、1本 400MB を超える。git add . の巻き添えで
# 公開リポジトリへ載る事故を、ここで止める。
test-data/
