# バージョンを固定する。以前は全 26 行が未固定で、docker build のたびに
# discord.py / fastapi / asyncpg などが最新へ動き、コードを1行も触っていない
# のに破壊的変更でデプロイが壊れる状態だった。
# 更新するときは、この行を書き換えるコミットとして明示的に行うこと。

# --- Discord Bot ---
discord.py[voice]==2.7.1
# 音声の「受信」は discord.py 本体に無い（play はあるが受信APIが存在しない）。
# VC録音のためだけに使う拡張で、VoiceRecvClient は discord.VoiceClient の
# サブクラスとして送信と受信を同時に持てるため、読み上げと接続を共有できる。
# 上流がまだ alpha なので、上げるときは録音の動作確認をしてからにすること。
discord-ext-voice-recv==0.5.2a179
# Discord は 2024年9月から音声を端から端まで暗号化している（DAVE）。
# 2026年3月2日からボットにも必須で、未対応だと音声接続そのものが
# クローズコード 4017 で拒否される。鍵交換（MLS）は discord.py 2.7.1 が
# 実装済みで、これが入っていれば自動で成立する。
# 受信側の復号は discord.py にも voice_recv にも無いので services/dave.py で行う。
davey==0.1.6

# --- 声の解析（録音が加工されていないかを調べる） ---
# LPC の極を解くのに使う。多項式の根＝共鳴で、角度が周波数、原点からの
# 距離が鋭さになる。包絡を刻んで山を拾う方法も試したが、余計な山を
# フォルマントと取り違えて使いものにならなかった。
# 2.5 系は Python 3.12 以上を要求する。本番は 3.11-slim なので 2.2 系で固定する
# （tools/check_requirements.py がこの食い違いを検出した）。
numpy==2.2.6
# discord.py[voice] が PyNaCl<1.6,>=1.5.0 を要求するため 1.5.0 で固定する。
# discord.py の voice extra を上げない限り、ここを 1.6 系にすると解決不能になる。
PyNaCl==1.5.0

# --- HTTP / 非同期 ---
aiohttp==3.14.3

# --- Web（管理画面・公開サイト） ---
fastapi==0.141.1
uvicorn==0.52.3
jinja2==3.1.6
itsdangerous==2.2.0
python-multipart==0.0.32
slowapi==0.1.10

# --- プロセス間で共有する状態（Valkey） ---
# Valkey へも同じプロトコルで話すので、クライアントは redis-py を使う。
#
# **8.0 系へ上げてはいけない。** slowapi が使う limits の redis ストレージが
# `redis!=4.5.2,!=4.5.3,<8.0.0` を要求するため、8 系にすると管理画面の
# レート制限を Valkey へ載せられなくなる（tools/check_requirements.py が
# この食い違いを検出する）。7.4.1 は制約を満たす最新版。
#
# 同期版（limits が使う）と非同期版（redis.asyncio、webapp のキャッシュが使う）が
# 同じパッケージに入っているので、ピンは1つで足りる。
redis==7.4.1

# --- DB ---
SQLAlchemy==2.0.52
asyncpg==0.31.0
alembic==1.19.1
apscheduler==3.11.3

# --- 外部サービス ---
groq==1.6.0
vt-py==0.22.0
pywebpush==2.4.0
cryptography==50.0.0

# --- 画像・音声 ---
# 12.2.0 は 11 件の勧告に該当していた（PcfFontFile の glyph 寸法、ImageCms の
# apply、PdfParser の展開爆弾など）。12.3.0 で全部修正されている。地震画像は
# 自分で描くだけだが、colorthief がサーバーのロゴ画像を読むので、復号側の
# 欠陥は外から与えられたデータに当たる。CI の pip-audit がこれを見つけた。
Pillow==12.3.0
mutagen==1.48.1

# --- 監視 ---
psutil==7.2.2

# --- 静的アセットの最小化 ---
rcssmin==1.2.2
rjsmin==1.2.5

# 以下はこの環境に入っていないため実バージョンを確認できず、メジャー範囲で
# 留めている。いずれも欠けても動く（orjson は json、colorthief は
# ロゴ色抽出の無効化にフォールバックする）。
orjson>=3,<4
colorthief>=0.2,<1
imageio-ffmpeg>=0.4,<1

# yt-dlp は配信サイト側の変更に追従する必要があるため、意図的に固定しない。
yt-dlp
